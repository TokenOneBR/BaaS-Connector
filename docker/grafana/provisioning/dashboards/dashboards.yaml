apiVersion: 1
providers:
  - name: baas
    folder: BaaS Connector
    type: file
    # `foldersFromFilesStructure` desligado: um painel so, e uma arvore de
    # pastas derivada do disco surpreende quem adiciona o segundo.
    options: { path: /var/lib/grafana/dashboards }
